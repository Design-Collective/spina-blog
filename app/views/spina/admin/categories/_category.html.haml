%li.dd-item
  %div.dd-item-inner
    = link_to spina.edit_admin_category_path(category), class: 'button button-link button-small sortable-link' do
      = category.title
    %span.pull-right
      = link_to spina.edit_admin_category_path(category), class: 'button button-small button-link dd-nodrag' do
        = icon('pencil-outline')
        = t('spina.edit')
